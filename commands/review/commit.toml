description = "Review a specific commit. Usage: /review:commit <sha>"
prompt = """
Conduct a code review of the following commit.

Commit: {{args}}

Commit message:
!{git log --format='%s%n%n%b' -1 {{args}}}

Changed files:
!{git show --name-only --format='' {{args}}}

Diff stat:
!{git show --stat --format='' {{args}}}

Diff:
!{git show {{args}}}

Review these changes following the methodology in your context. Print all findings to the terminal.
"""
