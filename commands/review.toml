description = "Review uncommitted code changes (staged + unstaged) with sub-agent delegation."
prompt = """
Conduct a code review of the following uncommitted changes.

Scope: uncommitted (staged + unstaged)

Changed files:
!{git diff --name-only HEAD}

Diff stat:
!{git diff --stat HEAD}

Diff:
!{git diff HEAD}

Review these changes following the methodology in your context. Print all findings to the terminal.
"""
